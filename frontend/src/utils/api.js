// Demo mode — serves all API routes from in-memory mock data (no real backend needed).
// mockApi provides the same interface as axios (get, post, put, delete, interceptors).
export { default } from './mockApi';
